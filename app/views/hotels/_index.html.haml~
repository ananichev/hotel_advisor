  <h2><%= f.title %></h2>
  <% if f.image_url %>
    <p><%= image_tag f.image_url(:thumb) %>
  <% else %>
    <%= image_tag("miss_image.jpg", alt: f.title) %>
  <% end %>
    <ul>
      <%= render "/hotels/info", g: f %>
    </ul>
  </p>
  <%= link_to "Details", hotel_path(f) %>

