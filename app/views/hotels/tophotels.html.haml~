<%= provide(:title, "Top Hotels")%>

<h1>Top Hotels</h1>
<% if signed_in? %>
  <%= link_to "Create new advise", new_hotel_path %>
<% end %>
<% @hotels.each do |f| %>
  <%= render "index", f: f %>
<% end %>
