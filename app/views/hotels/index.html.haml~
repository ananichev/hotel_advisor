<%= provide(:title, "Advises")%>

<h1>Hotels</h1>
<% if signed_in? %>
  <%= link_to "Create new advise", new_hotel_path %>
<% end %>
<% @hotels.each do |f| %>
  <%= render "index", f: f %>
<% end %>
<%= will_paginate @hotels %>
