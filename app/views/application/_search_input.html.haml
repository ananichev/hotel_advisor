= form_tag(hotels_search_path, method: :get, class: 'search-form') do
  = text_field_tag :query, params[:query], placeholder: 'Search by description'
  = submit_tag 'Search', name: nil, class: 'search-button'
%div{ style:'clear: both' }